var sign = fun(n){
    if(n < 0){
        return -1;
    } else if (n > 0){
        return 1;
    } else {
        return 0;
    }
    throw "Failed to sign";
};

var max = fun (a, b) {
  if(a > b) return a; 
  return b;
};

var abs = fun(a){
    if(a < 0) return a * -1;
    return a;
};